[ true* .
  (exists i:Nat . val(i < NUM_WAFERS) && load_stage(w(i))) .
  ( ! (exists h:Nat . val(h < NUM_WAFERS) && unload_stage(w(h))) )* .
  (exists j:Nat . val(j < NUM_WAFERS) && load_stage(w(j)))
] false
