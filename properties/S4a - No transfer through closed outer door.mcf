% S4a - No transfer through closed outer door
% Must have open_outer before any transfer_in
forall sid:SluiceId, i:Nat. val(validSluice(sid) && i < NUM_WAFERS) =>
  [!(open_outer(sid))* . transfer_in(w(i), sid)]true